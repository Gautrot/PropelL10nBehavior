name: GitHub Actions - CI branch main

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'
  workflow_dispatch:
permissions:
  contents: read

jobs:
  testsuite_linux:
    name: "Test Suite (Linux)"
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        php-version:
          - '7.4'
          - '8.0'
          - '8.1'
          - '8.2'
          - '8.3'
          - '8.4'
          - '8.5'
        symfony-version:
          - '5.4'

    steps:
      - name: Setup PHP, with composer and extensions
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240
        with:
          php-version: ${{ matrix.php-version }}
          #extensions: json, libxml, pdo, pdo_mysql, pdo_sqlite, pdo_pgsql, sqlite3
          coverage: pcov

      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Composer get cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Composer cache dependencies
        uses: actions/cache@v6
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.json') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Composer update (Symfony version ${{ matrix.symfony-version }})
        run: |
          composer i
        env:
          SYMFONY_REQUIRE: "${{ matrix.symfony-version }}"

      - name: Run tests with PHPs >= 8.1
        if: matrix.php-version >= '8.1'
        run: vendor/bin/phpunit --display-deprecations --display-notices --display-warnings

      - name: Run tests with PHPs < 8.1
        if: matrix.php-version < '8.1'
        run: vendor/bin/phpunit --verbose
  testsuite_win:
    name: "Test Suite (Windows)"
    runs-on: windows-2022
    defaults:
      run:
        shell: bash # https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#defaultsrunshell
    strategy:
      fail-fast: false
      matrix:
        php-version:
          - '7.4'
          - '8.0'
          - '8.1'
          - '8.2'
          - '8.3'
          - '8.4'
          - '8.5'
        symfony-version:
          - '5.4'

    steps:
      - name: Setup PHP, with composer and extensions
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240
        with:
          php-version: ${{ matrix.php-version }}
          extensions: pdo_sqlite
          coverage: pcov

      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Composer get cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Composer cache dependencies
        uses: actions/cache@v6
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.json') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Composer update (Symfony version ${{ matrix.symfony-version }})
        run: |
          composer i
        env:
          SYMFONY_REQUIRE: "${{ matrix.symfony-version }}"

      - name: Run tests with PHPs >= 8.1
        if: matrix.php-version >= '8.1'
        run: vendor/bin/phpunit --display-deprecations --display-notices --display-warnings

      - name: Run tests with PHPs < 8.1
        if: matrix.php-version < '8.1'
        run: vendor/bin/phpunit --verbose
  testsuite_macos:
    name: "Test Suite (macOS)"
    runs-on: macos-14
    strategy:
      fail-fast: false
      matrix:
        php-version:
          - '7.4'
          - '8.0'
          - '8.1'
          - '8.2'
          - '8.3'
          - '8.4'
          - '8.5'
        symfony-version:
          - '5.4'

    steps:
      - name: Setup PHP, with composer and extensions
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240
        with:
          php-version: ${{ matrix.php-version }}
          #extensions: json, libxml, pdo, pdo_mysql, pdo_sqlite, pdo_pgsql, sqlite3
          coverage: pcov

      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Composer get cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Composer cache dependencies
        uses: actions/cache@v6
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.json') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Composer update (Symfony version ${{ matrix.symfony-version }})
        run: |
          composer i
        env:
          SYMFONY_REQUIRE: "${{ matrix.symfony-version }}"

      - name: Run tests with PHPs >= 8.1
        if: matrix.php-version >= '8.1'
        run: vendor/bin/phpunit --display-deprecations --display-notices --display-warnings

      - name: Run tests with PHPs < 8.1
        if: matrix.php-version < '8.1'
        run: vendor/bin/phpunit --verbose
